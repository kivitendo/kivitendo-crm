/*globals require, process, console, exports */

(function () {

	'use strict';

	var cheerio = require('cheerio'),
		fs = require('fs'),
		doc = fs.readFileSync(__dirname + '/test/example-html/page1-caps.html', 'utf8'),
		$ = cheerio.load(doc, {lowerCaseTags: false}),
		$matches = $('li');

	console.log($matches);

}());
